<!DOCTYPE html>
<html lang="en">
<%- include('partials/head.partials.ejs') %>
<link rel="stylesheet" href="/styles/update.css">
<body>
<section class="update-container">
<button type="button" class="back-arrow" aria-label="Go back" onclick="window.history.back()">←</button>

<form action="/update/<%= blog._id %>" method="POST">
    
    <input type="hidden" name="blogId" value="<%= blog._id %>">
    
    <section class="form-group">
        <label for="title-input">Title</label>
        <input type="text" id="title-input" name="title" value="<%= blog.title %>">

        <div class="info">
            <div><p><%= blog.date %></p></div>
            <div><p><%= blog.owner.name %></p></div>
        </div>
    </section>
    
    <section class="form-group">
        <label for="description-input">Description</label>
        <textarea name="description" id="description-input" rows="4"><%= blog.description %></textarea>
    </section>

    <section class="form-group">
        <label for="content-input">Content</label>
        <!-- Include Quill stylesheet -->
        <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
        <div id="editor-container"><%- blog.content %></div>
        <input type="hidden" name="content" id="content-input">
    </section>

    <!-- Include the Quill library -->
    <script src="https://cdn.quilljs.com/1.3.6/quill.js"></script>

    <script>
        var quill = new Quill('#editor-container', {
            theme: 'snow',
            placeholder: 'Write your blog content here...',
            modules: {
                toolbar: [
                    [{ 'header': [1, 2, 3, false] }],
                    ['bold', 'italic', 'underline', 'strike', 'blockquote'],
                    [{ 'list': 'ordered'}, { 'list': 'bullet' }],
                    [{ 'align': [] }],
                    ['link'],
                    ['clean']
                ]
            }
        });

        var form = document.querySelector('form');
        var statusInput = document.querySelector('#status-input');
        var isDraft = <%= blog.status === 'draft' ? 'true' : 'false' %>;

        if (isDraft) {
            var publishBtn = document.querySelector('#publish-btn');
            var draftBtn = document.querySelector('#draft-btn');

            draftBtn.addEventListener('click', function() {
                statusInput.value = 'draft';
            });

            publishBtn.addEventListener('click', function() {
                statusInput.value = 'published';
            });
        }

        form.onsubmit = function(e) {
            var contentInput = document.querySelector('#content-input');
            contentInput.value = quill.root.innerHTML;

            if (!isDraft || statusInput.value === 'published') {
                if (quill.getText().trim().length === 0 && !quill.root.innerHTML.includes('<img')) {
                    e.preventDefault();
                    alert("Blog content cannot be empty.");
                    return false;
                }
            }
        };
    </script>

    <% if (blog.status === 'draft') { %>
        <input type="hidden" name="status" id="status-input" value="draft">
        <div class="submit-section">
            <button type="submit" id="draft-btn" class="secondary-btn">Save Draft</button>
            <button type="submit" id="publish-btn" class="primary-btn">Publish</button>
        </div>
    <% } else { %>
        <div class="submit-section">
            <button type="submit" class="primary-btn">Update Blog</button>
        </div>
    <% } %>

</form>
</section>

    

    
</body>
</html>
