<!DOCTYPE html>
<html lang="en">
<%- include('partials/head.partials.ejs') %>
<link rel="stylesheet" href="/styles/editor-form.css">
<link rel="stylesheet" href="/styles/createblog.css">
<link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
<body class="editor-body">
    <section class="editor-page">
        <header class="editor-header">
            <a href="/drafts" class="back-link" aria-label="Back to drafts">← Back</a>
            <h1>Create a Blog</h1>
            <p class="editor-subtitle">Write your post and save as a draft or publish when ready.</p>
        </header>

        <div class="editor-card">
            <form id="editor-form" action="/createblog" method="POST">
                <fieldset class="form-section">
                    <legend>Post details</legend>

                    <div class="field-group">
                        <label for="title-input">Title</label>
                        <input id="title-input" type="text" name="title" placeholder="Give your post a title">
                    </div>

                    <div class="field-group">
                        <label for="desc-input">Description</label>
                        <textarea id="desc-input" name="description" rows="3" placeholder="A short summary shown in the archive"></textarea>
                        <p class="field-hint">This appears on the home page and archive listing.</p>
                    </div>
                </fieldset>

                <fieldset class="form-section">
                    <legend>Content</legend>
                    <div id="editor-container"></div>
                    <input type="hidden" name="content" id="content-input">
                </fieldset>

                <input type="hidden" name="ownerId" value="<%= user._id %>">
            </form>
        </div>

        <div class="editor-actions">
            <p class="publish-hint">Publishing requires title, description, and content.</p>
            <div class="action-buttons">
                <button type="submit" form="editor-form" name="status" value="draft" class="secondary-btn">Save as Draft</button>
                <button type="submit" form="editor-form" name="status" value="published" class="primary-btn">Publish</button>
            </div>
        </div>
    </section>

    <%- include('partials/navigation.partial.ejs') %>

    <script src="https://cdn.quilljs.com/1.3.6/quill.js"></script>
    <script>
        var quill = new Quill('#editor-container', {
            theme: 'snow',
            placeholder: 'Write your blog content here...',
            modules: {
                toolbar: [
                    [{ 'header': [1, 2, 3, false] }],
                    ['bold', 'italic', 'underline', 'strike', 'blockquote'],
                    [{ 'list': 'ordered'}, { 'list': 'bullet' }],
                    [{ 'align': [] }],
                    ['link'],
                    ['clean']
                ]
            }
        });

        document.getElementById('editor-form').onsubmit = function(e) {
            document.getElementById('content-input').value = quill.root.innerHTML;

            var isPublishing = document.activeElement?.name === 'status' && document.activeElement?.value === 'published';
            if (isPublishing && quill.getText().trim().length === 0 && !quill.root.innerHTML.includes('<img')) {
                e.preventDefault();
                alert("Blog content cannot be empty.");
                return false;
            }
        };
    </script>
</body>
</html>
